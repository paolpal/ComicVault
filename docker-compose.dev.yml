services:
  comic-vault:
    build:
      target: development
    environment:
      - FLASK_APP=run.py
      - FLASK_DEBUG=1
    volumes:
      - .:/app
      - ${COMICS_VOLUME_PATH:-${HOME}/Comics}:/data/comics
    command: python -m flask run --host=0.0.0.0 --port=5000 --reload
